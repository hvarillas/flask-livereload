[project]
name = "flask-livereload"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "Flask>=3.1",
    "watchdog",
]

[tool.setuptools.packages.find]
where = ["flask_livereload/src"]
